#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main()
{
    int n, i, j;
    cin >> n;
    int arr[n];
    for (i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    int a, b, sum = 0;
    while(n=0)
    {
        for (int j = 0; j <= 3; j++)
        {
            sum = sum + arr[i];
        }
    }
    cout<<sum<<endl;
    if (sum == 0)
    {
        a = 1;
        cout << a;
    }
    else
    {
        b = 0;
        cout << b;
    }

    return 0;
}
