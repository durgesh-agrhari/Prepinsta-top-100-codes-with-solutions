#include <bits/stdc++.h>
using namespace std;

int main()
{
    char s;
    cin >> s;
    if (s == 'a' || s == 'e' || s == 'i' || s == 'o' || s == 'u')
    {
        cout << "Vowel";
    }
    else
    {
        cout << "consonant";
    }

    return 0;
}
